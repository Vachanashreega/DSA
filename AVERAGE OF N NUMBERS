#include<stdio.h>
int main() 
{
    int numbers[100], sum = 0;
    float average;
    int i;
    for (i = 0; i < 100; i++)
    {
        sum = sum + numbers[i];
    }
    average = (float)sum / i;
    printf("Average = %0.2f\n", average);
    return 0;
}
